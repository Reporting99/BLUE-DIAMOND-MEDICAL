/** Contact domain: the form, and the schema that validates it on both sides. */
export { ContactForm } from "./components/ContactForm";
export { contactFormSchema, sanitizeText, type ContactFormValues } from "./validation";
